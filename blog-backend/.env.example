# 与现有 Java 后端共用同一套 MySQL 和 Redis，迁移期默认端口为 8082。
APP_ENV=development
DATABASE_URL=mysql+asyncmy://root:123456@localhost:3306/eleven_blog?charset=utf8mb4
REDIS_URL=redis://:123456@localhost:6379/1
SCHEDULED_TASKS_ENABLED=false
